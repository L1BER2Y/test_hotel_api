spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotels_db?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

  h2:
    console:
      enabled: false
